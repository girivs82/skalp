// =============================================================================
// Motor Control Unit (MCU) v4 - Command Processor Redundancy
// =============================================================================
// Changes from v3:
//   - Dual Command Processor with comparison
//   - Added force-to-safe on any SM fault
// =============================================================================

// =============================================================================
// Command Processor Channel (single instance for dual)
// =============================================================================
entity CommandChannel {
    in clk: clock
    in rst: bit
    in cmd_valid: bit
    in cmd_throttle: bit[8]
    in cmd_enable: bit
    out validated_throttle: bit[8]
    out validated_enable: bit
    out cmd_error: bit
}

impl CommandChannel {
    signal throttle_reg: bit[8] = 0
    signal enable_reg: bit = 0
    signal error_reg: bit = 0

    signal throttle_diff: bit[8] = if cmd_throttle > throttle_reg {
        cmd_throttle - throttle_reg
    } else {
        throttle_reg - cmd_throttle
    }
    signal rate_ok: bit = if throttle_diff > 16 { 0 } else { 1 }

    on(clk.rise) {
        if (rst) {
            throttle_reg = 0
            enable_reg = 0
            error_reg = 0
        } else if (cmd_valid) {
            if (rate_ok) {
                throttle_reg = cmd_throttle
                enable_reg = cmd_enable
                error_reg = 0
            } else {
                error_reg = 1
            }
        }
    }

    validated_throttle = throttle_reg
    validated_enable = enable_reg
    cmd_error = error_reg
}

// =============================================================================
// 8-bit Comparator with Fault Detection
// =============================================================================
#[safety_mechanism(type=comparator)]
entity ByteComparator {
    in a: bit[8]
    in b: bit[8]
    out matched: bit[8]
    #[detection_signal]
    out mismatch: bit
}

impl ByteComparator {
    matched = a
    mismatch = if a == b { 0 } else { 1 }
}

// =============================================================================
// Dual Command Processor with Comparison
// =============================================================================
#[implements(SG001::CommandValidation)]
#[safety_mechanism(type=comparator)]
entity DualCommandProcessor {
    in clk: clock
    in rst: bit
    in cmd_valid: bit
    in cmd_throttle: bit[8]
    in cmd_enable: bit
    out validated_throttle: bit[8]
    out validated_enable: bit
    #[detection_signal]
    out cmd_error: bit
    #[detection_signal]
    out cmd_fault: bit
}

impl DualCommandProcessor {
    signal throttle_a: bit[8]
    signal throttle_b: bit[8]
    signal enable_a: bit
    signal enable_b: bit
    signal error_a: bit
    signal error_b: bit
    signal checked_throttle: bit[8]
    signal throttle_mismatch: bit

    inst ch_a = CommandChannel {
        clk: clk,
        rst: rst,
        cmd_valid: cmd_valid,
        cmd_throttle: cmd_throttle,
        cmd_enable: cmd_enable,
    }
    throttle_a = ch_a.validated_throttle
    enable_a = ch_a.validated_enable
    error_a = ch_a.cmd_error

    inst ch_b = CommandChannel {
        clk: clk,
        rst: rst,
        cmd_valid: cmd_valid,
        cmd_throttle: cmd_throttle,
        cmd_enable: cmd_enable,
    }
    throttle_b = ch_b.validated_throttle
    enable_b = ch_b.validated_enable
    error_b = ch_b.cmd_error

    inst throttle_cmp = ByteComparator {
        a: throttle_a,
        b: throttle_b,
    }
    checked_throttle = throttle_cmp.matched
    throttle_mismatch = throttle_cmp.mismatch

    validated_throttle = checked_throttle
    validated_enable = enable_a

    // Command error from either channel
    cmd_error = error_a | error_b

    // Comparison fault detection
    signal enable_mismatch: bit = if enable_a == enable_b { 0 } else { 1 }
    signal error_mismatch: bit = if error_a == error_b { 0 } else { 1 }
    cmd_fault = throttle_mismatch | enable_mismatch | error_mismatch
}

// =============================================================================
// PWM Channel
// =============================================================================
entity PWMChannel {
    in clk: clock
    in rst: bit
    in throttle: bit[8]
    in enable: bit
    in force_off: bit
    out pwm_out: bit
    out pwm_active: bit
}

impl PWMChannel {
    signal counter: bit[8] = 0
    signal pwm_reg: bit = 0

    on(clk.rise) {
        if (rst) {
            counter = 0
            pwm_reg = 0
        } else {
            counter = counter + 1
            if (force_off) {
                pwm_reg = 0
            } else if (enable) {
                pwm_reg = if counter < throttle { 1 } else { 0 }
            } else {
                pwm_reg = 0
            }
        }
    }

    pwm_out = pwm_reg
    pwm_active = enable & ~force_off
}

// =============================================================================
// Dual PWM Generator
// =============================================================================
#[safety_mechanism(type=comparator)]
entity DualPWMGenerator {
    in clk: clock
    in rst: bit
    in throttle: bit[8]
    in enable: bit
    in force_off: bit
    out pwm_out: bit
    out pwm_active: bit
    #[detection_signal]
    out pwm_fault: bit
}

impl DualPWMGenerator {
    signal pwm_a: bit
    signal pwm_b: bit
    signal active_a: bit
    signal active_b: bit

    inst ch_a = PWMChannel {
        clk: clk,
        rst: rst,
        throttle: throttle,
        enable: enable,
        force_off: force_off,
    }
    pwm_a = ch_a.pwm_out
    active_a = ch_a.pwm_active

    inst ch_b = PWMChannel {
        clk: clk,
        rst: rst,
        throttle: throttle,
        enable: enable,
        force_off: force_off,
    }
    pwm_b = ch_b.pwm_out
    active_b = ch_b.pwm_active

    pwm_out = pwm_a
    pwm_active = active_a
    signal pwm_mismatch: bit = if pwm_a == pwm_b { 0 } else { 1 }
    signal active_mismatch: bit = if active_a == active_b { 0 } else { 1 }
    pwm_fault = pwm_mismatch | active_mismatch
}

// =============================================================================
// Current Channel
// =============================================================================
entity CurrentChannel {
    in clk: clock
    in rst: bit
    in current_adc: bit[10]
    in threshold: bit[10]
    out overcurrent: bit
    out current_valid: bit
}

impl CurrentChannel {
    signal oc_reg: bit = 0
    signal valid_reg: bit = 0
    signal filter_count: bit[4] = 0

    on(clk.rise) {
        if (rst) {
            oc_reg = 0
            valid_reg = 0
            filter_count = 0
        } else {
            valid_reg = 1
            if (current_adc > threshold) {
                if (filter_count < 8) {
                    filter_count = filter_count + 1
                }
                if (filter_count >= 8) {
                    oc_reg = 1
                }
            } else {
                filter_count = 0
                oc_reg = 0
            }
        }
    }

    overcurrent = oc_reg
    current_valid = valid_reg
}

// =============================================================================
// Bit Voter
// =============================================================================
#[safety_mechanism(type=tmr)]
entity BitVoter {
    in a: bit
    in b: bit
    in c: bit
    out voted: bit
    #[detection_signal]
    out fault: bit
}

impl BitVoter {
    signal ab: bit = a & b
    signal bc: bit = b & c
    signal ac: bit = a & c
    voted = ab | bc | ac
    signal all_same: bit = if a == b { if b == c { 1 } else { 0 } } else { 0 }
    fault = if all_same { 0 } else { 1 }
}

// =============================================================================
// TMR Current Monitor
// =============================================================================
#[implements(SG003::OvercurrentDetection)]
#[safety_mechanism(type=tmr)]
entity TmrCurrentMonitor {
    in clk: clock
    in rst: bit
    in current_adc: bit[10]
    in threshold: bit[10]
    #[detection_signal]
    out overcurrent: bit
    out current_valid: bit
    #[detection_signal]
    out tmr_fault: bit
}

impl TmrCurrentMonitor {
    signal oc_a: bit
    signal oc_b: bit
    signal oc_c: bit
    signal valid_a: bit
    signal valid_b: bit
    signal valid_c: bit
    signal voted_oc: bit
    signal voted_valid: bit
    signal fault_oc: bit
    signal fault_valid: bit

    inst ch_a = CurrentChannel {
        clk: clk,
        rst: rst,
        current_adc: current_adc,
        threshold: threshold,
    }
    oc_a = ch_a.overcurrent
    valid_a = ch_a.current_valid
    inst ch_b = CurrentChannel {
        clk: clk,
        rst: rst,
        current_adc: current_adc,
        threshold: threshold,
    }
    oc_b = ch_b.overcurrent
    valid_b = ch_b.current_valid
    inst ch_c = CurrentChannel {
        clk: clk,
        rst: rst,
        current_adc: current_adc,
        threshold: threshold,
    }
    oc_c = ch_c.overcurrent
    valid_c = ch_c.current_valid

    inst voter_oc = BitVoter {
        a: oc_a,
        b: oc_b,
        c: oc_c,
    }
    voted_oc = voter_oc.voted
    fault_oc = voter_oc.fault
    inst voter_valid = BitVoter {
        a: valid_a,
        b: valid_b,
        c: valid_c,
    }
    voted_valid = voter_valid.voted
    fault_valid = voter_valid.fault

    overcurrent = voted_oc
    current_valid = voted_valid
    tmr_fault = fault_oc | fault_valid
}

// =============================================================================
// Watchdog Channel
// =============================================================================
entity WatchdogChannel {
    in clk: clock
    in rst: bit
    in kick: bit
    in timeout_limit: bit[8]
    out timeout: bit
    out healthy: bit
}

impl WatchdogChannel {
    signal counter: bit[8] = 0
    signal timeout_reg: bit = 0

    on(clk.rise) {
        if (rst) {
            counter = 0
            timeout_reg = 0
        } else if (kick) {
            counter = 0
            timeout_reg = 0
        } else {
            if (counter < timeout_limit) {
                counter = counter + 1
            } else {
                timeout_reg = 1
            }
        }
    }

    timeout = timeout_reg
    healthy = ~timeout_reg
}

// =============================================================================
// Dual Watchdog
// =============================================================================
#[safety_mechanism(type=comparator)]
entity DualWatchdog {
    in clk: clock
    in rst: bit
    in kick: bit
    in timeout_limit: bit[8]
    #[detection_signal]
    out timeout: bit
    out healthy: bit
    #[detection_signal]
    out watchdog_fault: bit
}

impl DualWatchdog {
    signal timeout_a: bit
    signal timeout_b: bit
    signal healthy_a: bit
    signal healthy_b: bit

    inst wd_a = WatchdogChannel {
        clk: clk,
        rst: rst,
        kick: kick,
        timeout_limit: timeout_limit,
    }
    timeout_a = wd_a.timeout
    healthy_a = wd_a.healthy
    inst wd_b = WatchdogChannel {
        clk: clk,
        rst: rst,
        kick: kick,
        timeout_limit: timeout_limit,
    }
    timeout_b = wd_b.timeout
    healthy_b = wd_b.healthy

    timeout = timeout_a
    healthy = healthy_a
    signal mismatch: bit = if timeout_a == timeout_b { 0 } else { 1 }
    watchdog_fault = mismatch
}

// =============================================================================
// Safe State Channel
// =============================================================================
entity SafeStateChannel {
    in clk: clock
    in rst: bit
    in fault_cmd: bit
    in fault_overcurrent: bit
    in fault_watchdog: bit
    in fault_clear: bit
    out safe_state: bit
    out force_motor_off: bit
    out fault_latched: bit
}

impl SafeStateChannel {
    signal fault_latch: bit = 0
    signal safe_state_reg: bit = 1
    signal any_fault: bit = fault_cmd | fault_overcurrent | fault_watchdog

    on(clk.rise) {
        if (rst) {
            fault_latch = 0
            safe_state_reg = 1
        } else {
            if (any_fault) {
                fault_latch = 1
                safe_state_reg = 1
            } else if (fault_clear & ~any_fault) {
                fault_latch = 0
                safe_state_reg = 0
            }
        }
    }

    safe_state = safe_state_reg
    force_motor_off = safe_state_reg | any_fault
    fault_latched = fault_latch
}

// =============================================================================
// Safe State Voter with SM-of-SM
// =============================================================================
#[safety_mechanism(type=tmr)]
entity SafeStateVoterDual {
    in a: bit
    in b: bit
    in c: bit
    out voted: bit
    #[detection_signal]
    out fault: bit
}

impl SafeStateVoterDual {
    signal ab1: bit = a & b
    signal bc1: bit = b & c
    signal ac1: bit = a & c
    signal voted1: bit = ab1 | bc1 | ac1
    signal voted2: bit = if a == b { a } else { c }

    voted = voted1
    signal all_same: bit = if a == b { if b == c { 1 } else { 0 } } else { 0 }
    signal voter_agree: bit = if voted1 == voted2 { 1 } else { 0 }
    fault = if all_same { if voter_agree { 0 } else { 1 } } else { 1 }
}

// =============================================================================
// TMR Safe State Controller
// =============================================================================
#[implements(SG001::SafeStateManagement)]
#[implements(SG002::SafeStateManagement)]
#[safety_mechanism(type=tmr)]
entity TmrSafeStateController {
    in clk: clock
    in rst: bit
    in fault_cmd: bit
    in fault_overcurrent: bit
    in fault_watchdog: bit
    in fault_sm: bit
    in fault_clear: bit
    out safe_state: bit
    out force_motor_off: bit
    #[detection_signal]
    out fault_latched: bit
    #[detection_signal]
    out tmr_fault: bit
}

impl TmrSafeStateController {
    signal ss_a: bit
    signal ss_b: bit
    signal ss_c: bit
    signal fmo_a: bit
    signal fmo_b: bit
    signal fmo_c: bit
    signal fl_a: bit
    signal fl_b: bit
    signal fl_c: bit
    signal voted_ss: bit
    signal voted_fmo: bit
    signal voted_fl: bit
    signal fault_ss: bit
    signal fault_fmo: bit
    signal fault_fl: bit

    // Combined fault input includes SM faults
    signal combined_fault: bit = fault_cmd | fault_sm

    inst ch_a = SafeStateChannel {
        clk: clk,
        rst: rst,
        fault_cmd: combined_fault,
        fault_overcurrent: fault_overcurrent,
        fault_watchdog: fault_watchdog,
        fault_clear: fault_clear,
    }
    ss_a = ch_a.safe_state
    fmo_a = ch_a.force_motor_off
    fl_a = ch_a.fault_latched
    inst ch_b = SafeStateChannel {
        clk: clk,
        rst: rst,
        fault_cmd: combined_fault,
        fault_overcurrent: fault_overcurrent,
        fault_watchdog: fault_watchdog,
        fault_clear: fault_clear,
    }
    ss_b = ch_b.safe_state
    fmo_b = ch_b.force_motor_off
    fl_b = ch_b.fault_latched
    inst ch_c = SafeStateChannel {
        clk: clk,
        rst: rst,
        fault_cmd: combined_fault,
        fault_overcurrent: fault_overcurrent,
        fault_watchdog: fault_watchdog,
        fault_clear: fault_clear,
    }
    ss_c = ch_c.safe_state
    fmo_c = ch_c.force_motor_off
    fl_c = ch_c.fault_latched

    inst voter_ss = SafeStateVoterDual {
        a: ss_a,
        b: ss_b,
        c: ss_c,
    }
    voted_ss = voter_ss.voted
    fault_ss = voter_ss.fault
    inst voter_fmo = SafeStateVoterDual {
        a: fmo_a,
        b: fmo_b,
        c: fmo_c,
    }
    voted_fmo = voter_fmo.voted
    fault_fmo = voter_fmo.fault
    inst voter_fl = SafeStateVoterDual {
        a: fl_a,
        b: fl_b,
        c: fl_c,
    }
    voted_fl = voter_fl.voted
    fault_fl = voter_fl.fault

    safe_state = voted_ss
    force_motor_off = voted_fmo
    fault_latched = voted_fl
    tmr_fault = fault_ss | fault_fmo | fault_fl
}

// =============================================================================
// Top-Level: Motor Control Unit v4
// =============================================================================
entity MotorControlUnitV4 {
    in clk: clock
    in rst: bit
    in cmd_valid: bit
    in cmd_throttle: bit[8]
    in cmd_enable: bit
    in current_adc: bit[10]
    in overcurrent_threshold: bit[10]
    in watchdog_timeout: bit[8]
    in watchdog_kick: bit
    in fault_clear: bit

    out motor_pwm: bit
    out motor_active: bit
    out system_healthy: bit
    out in_safe_state: bit
    #[detection_signal]
    out fault_status: bit
}

impl MotorControlUnitV4 {
    signal val_throttle: bit[8]
    signal val_enable: bit
    signal cmd_err: bit
    signal cmd_fault: bit

    signal pwm_signal: bit
    signal pwm_is_active: bit
    signal pwm_err: bit

    signal oc_detected: bit
    signal current_ok: bit
    signal curr_tmr_err: bit

    signal wd_timeout: bit
    signal wd_healthy: bit
    signal wd_err: bit

    signal safe_state: bit
    signal force_off: bit
    signal fault_latch: bit
    signal ss_tmr_err: bit

    // All SM faults aggregated
    signal all_sm_faults: bit = cmd_fault | pwm_err | curr_tmr_err | wd_err

    inst cmd_proc = DualCommandProcessor {
        clk: clk,
        rst: rst,
        cmd_valid: cmd_valid,
        cmd_throttle: cmd_throttle,
        cmd_enable: cmd_enable,
    }
    val_throttle = cmd_proc.validated_throttle
    val_enable = cmd_proc.validated_enable
    cmd_err = cmd_proc.cmd_error
    cmd_fault = cmd_proc.cmd_fault

    inst pwm_gen = DualPWMGenerator {
        clk: clk,
        rst: rst,
        throttle: val_throttle,
        enable: val_enable,
        force_off: force_off,
    }
    pwm_signal = pwm_gen.pwm_out
    pwm_is_active = pwm_gen.pwm_active
    pwm_err = pwm_gen.pwm_fault

    inst curr_mon = TmrCurrentMonitor {
        clk: clk,
        rst: rst,
        current_adc: current_adc,
        threshold: overcurrent_threshold,
    }
    oc_detected = curr_mon.overcurrent
    current_ok = curr_mon.current_valid
    curr_tmr_err = curr_mon.tmr_fault

    inst watchdog = DualWatchdog {
        clk: clk,
        rst: rst,
        kick: watchdog_kick,
        timeout_limit: watchdog_timeout,
    }
    wd_timeout = watchdog.timeout
    wd_healthy = watchdog.healthy
    wd_err = watchdog.watchdog_fault

    inst safe_ctrl = TmrSafeStateController {
        clk: clk,
        rst: rst,
        fault_cmd: cmd_err,
        fault_overcurrent: oc_detected,
        fault_watchdog: wd_timeout,
        fault_sm: all_sm_faults,
        fault_clear: fault_clear,
    }
    safe_state = safe_ctrl.safe_state
    force_off = safe_ctrl.force_motor_off
    fault_latch = safe_ctrl.fault_latched
    ss_tmr_err = safe_ctrl.tmr_fault

    motor_pwm = pwm_signal
    motor_active = pwm_is_active
    system_healthy = wd_healthy & current_ok & ~cmd_err & ~all_sm_faults & ~ss_tmr_err
    in_safe_state = safe_state
    fault_status = fault_latch | all_sm_faults | ss_tmr_err
}
