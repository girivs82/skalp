// Test impl_style::auto intent (default behavior) using new 'with intent::' syntax
entity TestImplStyleAuto {
    in a: bit[32]
    in b: bit[32]
    out result: bit[32]
}

impl TestImplStyleAuto {
    let tmp = (a | b) with intent::impl_style_auto;
    result = tmp
}
