// Test impl_style::tree intent using new 'with intent::' syntax
entity TestImplStyleTree {
    in a: bit[32]
    in b: bit[32]
    out result: bit[32]
}

impl TestImplStyleTree {
    let tmp = (a * b) with intent::impl_style_tree;
    result = tmp
}
