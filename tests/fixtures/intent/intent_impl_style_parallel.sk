// Test impl_style::parallel intent using new 'with intent::' syntax
entity TestImplStyleParallel {
    in a: bit[32]
    in b: bit[32]
    out result: bit[32]
}

impl TestImplStyleParallel {
    let tmp = (a + b) with intent::impl_style_parallel;
    result = tmp
}
