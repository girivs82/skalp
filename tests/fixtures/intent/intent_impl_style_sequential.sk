// Test impl_style::sequential intent using new 'with intent::' syntax
entity TestImplStyleSequential {
    in a: bit[32]
    in b: bit[32]
    out result: bit[32]
}

impl TestImplStyleSequential {
    let tmp = (a - b) with intent::impl_style_sequential;
    result = tmp
}
