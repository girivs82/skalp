[package]
name = "skalp-lir"
version = "0.1.0"
edition = "2021"

[dependencies]
petgraph = "0.6"
bitvec = "1.0"
indexmap = "2.0"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
skalp-mir = { path = "../skalp-mir" }