[package]
name = "skalp-stdlib"
version = "0.2.0"
edition = "2021"
description = "Standard library components for SKALP"

[dependencies]
# No dependencies - this is just SKALP source files

[features]
default = []
simulation = []
synthesis = []
